logging:
  level:
    org.flywaydb: INFO
    org.springframework: INFO

server:
  port: 8080
  servlet:
    context-path: /v1/api

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/realmeet?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource
